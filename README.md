# 🚀 Docker & Jenkins CI/CD Pipeline (Quality Gate)

Этот проект демонстрирует настройку автоматизированного CI/CD пайплайна для Python-приложения. Основная цель — показать интеграцию **Docker** и **Jenkins** с внедрением автоматического контроля качества кода (**Quality Gate**).

Проект является частью портфолио по направлению **QA Automation & DevOps**.

---

## 📋 Основные возможности

* **🔍 Static Code Analysis (Linting):**
    Автоматическая проверка стиля кода с помощью `flake8`. Если код не соответствует стандартам PEP8, сборка останавливается до создания Docker-образа.
* **🐳 Containerization:**
    Упаковка Python-приложения в изолированный Docker-контейнер.
* **🤖 Automated Pipeline:**
    Полный цикл CI/CD: от проверки кода до запуска и тестирования контейнера.
* **🧹 Auto-Cleanup:**
    Автоматическая очистка системы от временных образов после завершения тестов.

---

## 🛠 Технологический стек

* **Язык:** Python 3.x
* **CI/CD:** Jenkins (Pipeline as Code)
* **Контейнеризация:** Docker
* **Линтинг:** Flake8
* **ОС:** Windows / Linux (Jenkinsfile адаптирован под Windows агенты)
* **VCS:** Git / GitHub

---

## 📈 Архитектура пайплайна (Jenkinsfile)

Пайплайн состоит из 5 последовательных этапов:

1.  **Checkout:**
    Jenkins загружает актуальную версию кода из репозитория GitHub.
2.  **Lint Code (Quality Gate):**
    Запуск `flake8 app.py`.
    * *Успех:* Переход к сборке.
    * *Ошибка:* Остановка пайплайна (Fail).
3.  **Build Image:**
    Сборка образа `shoxrux-app` с уникальным тегом сборки.
4.  **Test Run:**
    Запуск контейнера в тестовом режиме для проверки вывода приложения ("Smoke Test").
5.  **Clean Up:**
    Удаление созданного образа для освобождения дискового пространства.

---

## ⚙️ Как запустить локально

Если вы хотите протестировать работу приложения без Jenkins:

1.  **Клонируйте репозиторий:**
    ```bash
    git clone [https://github.com/TokhirjonYuldoshev/my-docker-project.git](https://github.com/TokhirjonYuldoshev/my-docker-project.git)
    cd my-docker-project
    ```

2.  **Установите зависимости (опционально для проверки линтера):**
    ```bash
    pip install flake8
    python -m flake8 app.py
    ```

3.  **Соберите Docker-образ:**
    ```bash
    docker build -t my-python-app .
    ```

4.  **Запустите контейнер:**
    ```bash
    docker run --rm my-python-app
    ```

---

## 📂 Структура проекта

```text
my-docker-project/
├── app.py          # Основной скрипт Python
├── Dockerfile      # Инструкция для сборки образа
├── Jenkinsfile     # Сценарий CI/CD пайплайна (Groovy)
└── README.md       # Документация проекта
